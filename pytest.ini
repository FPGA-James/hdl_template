[pytest]
# Test discovery root — all pytest test files live here
testpaths = scripts/hdl_autodoc/tests

# Add the hdl_autodoc package to sys.path so tests can import pipeline modules
# without needing the conftest.py sys.path hack (pytest 7+ supports this natively)
pythonpath = scripts/hdl_autodoc
