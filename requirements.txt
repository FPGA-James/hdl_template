# HDL Template Python dependencies.
# Install via: make venv (creates .venv/) or pip install -r requirements.txt

# ── Verification frameworks ───────────────────────────────────────────────────
# vunit-hdl: pinned to the 5.0.0 pre-release line (not >=4.7) because
# hdl-modules' bfm/axi_lite_master.vhd (used by tb/vunit/run.py to drive
# <name>_top's real AXI-Lite register interface) is only compatible with
# VUnit >= 5.0.0 -- hdl-modules documents this explicitly ("Add note on
# pre-release VUnit", hdl-modules commit f7ccb1a, getting_started.rst).
# VUnit 5.0.0 is not yet on a stable PyPI release, but its pre-release
# builds are: `pip install "vunit-hdl>=5.0.0.dev5"` resolves to one without
# needing --pre, since the specifier itself names a pre-release version.
# Verified: 5.0.0.dev11 compiles and runs the VUnit testbench cleanly.
# Note the VUnit 5 API change this also required in tb/vunit/run.py:
# VUnit.from_argv() no longer takes compile_builtins= (call
# add_vhdl_builtins() explicitly instead, which run.py already does).
vunit-hdl>=5.0.0.dev5
cocotb>=2.0

# ── Register generation ───────────────────────────────────────────────────────
hdl-registers>=8.0
# SystemVerilog register-file generation (hdl_registers' SV generator delegates to this)
peakrdl-regblock>=1.0

# ── VHDL linting ─────────────────────────────────────────────────────────────
# vsg (VHDL Style Guide): style/convention linter configured via vsg.yml
vsg>=3.0

# ── Sphinx documentation (HDL AutoDoc pipeline) ───────────────────────────────
sphinx>=7.0,<9.0

# Pin docutils — sphinx-vhdl 0.2.x has a transition-node bug with docutils 0.21+
docutils>=0.18,<0.21

furo>=2025.12.19

# WaveDrom timing diagrams via .. wavedrom:: directive in VHDL comments
sphinxcontrib-wavedrom>=3.0

# VHDL domain — enables :vhdl: cross-references in Sphinx
sphinx-vhdl>=0.1

# Offline WaveDrom rendering (no Node.js required for PDF builds)
wavedrom

# SVG → PDF conversion for LaTeX/PDF builds (schematic and logo SVGs)
# Provides sphinxcontrib.rsvgconverter; [cairosvg] pulls in the pure-Python backend
sphinxcontrib-svg2pdfconverter[cairosvg]>=1.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0
